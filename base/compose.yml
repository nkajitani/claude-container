services:
  claude:
    build: .
    container_name: claude
    volumes:
      - ~/docker/property_analyze:/app
      - ./.claude:/root/.claude
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
